import React from 'react';
import {RefreshControl, RefreshControlProps} from 'react-native';

const RefreshControlPreview = (props: RefreshControlProps) => {
  return <RefreshControl {...props} />;
};

export {RefreshControlPreview};
